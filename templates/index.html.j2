<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitHub Alternative Trends</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header>
        <h1>GitHub Alternative Trends</h1>
        <p>AI/LLM以外の急成長中・個人/小規模開発リポジトリ</p>
        <p class="last-updated">Last updated: {{ last_updated }}</p>
    </header>

    <main>
        {% if repositories %}
            <ul class="repo-list">
                {% for repo in repositories %}
                    <li class="repo-item">
                        <h2><a href="{{ repo.url }}" target="_blank" rel="noopener noreferrer">{{ repo.name }}</a></h2>
                        <p class="description">{{ repo.description }}</p>
                        {% if repo.description_ja and repo.description_ja != repo.description %}
                        <p class="description-ja">🇯🇵 {{ repo.description_ja }}</p>
                        {% endif %}
                        <div class="meta">
                            <span class="language">💻 {{ repo.language or 'N/A' }}</span>
                            <span class="stars">⭐ {{ repo.stars }}</span>
                            <!-- <span class="stars-growth">📈 +{{ repo.stars_last_7_days }}</span>  Placeholder, so commented out -->
                            <span class="contributors">👥 {{ repo.contributors }}</span>
                            {% if repo.topics %}
                            <div class="topics">
                                Topics:
                                {% for topic in repo.topics %}
                                    <span class="topic-tag">{{ topic }}</span>
                                {% endfor %}
                            </div>
                            {% endif %}
                        </div>
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p>現在、表示できるリポジトリはありません。</p>
        {% endif %}
    </main>

    <footer>
        <!-- Footer content can go here if needed -->
    </footer>
</body>
</html>
